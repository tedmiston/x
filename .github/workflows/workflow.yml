name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v1
      - uses: docker://bats/bats:latest
        with:
          args: addition.bats
